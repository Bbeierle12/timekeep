# Timekeep Mobile

Expo-based mobile app scaffold for employees.

## Setup
1. Copy `apps/mobile/.env.example` to `apps/mobile/.env`.
2. Install dependencies at the repo root: `npm install`.
3. Start the app: `npm --workspace apps/mobile run start`.
